# R00

- UTC timestamp: 2026-03-20T00:00:00Z
- Frozen the current Markdown/prose manuscript line by adding `docs/project/prose_line_freeze.yaml` with hash-and-size entries for the draft, writing-track, and supporting project manifests.
- The freeze validator is `python3 scripts/run_r00_freeze_and_open_rewrite_lane.py`; it creates the manifest if missing and otherwise fails on any frozen-file hash drift.
- Opened a parallel LaTeX rewrite lane under `paper/rewrite_tex/`; this is a theorem-paper skeleton for the rewrite, not a patch to the frozen prose line.
- Exact build command: `latexmk -pdf -bibtex -interaction=nonstopmode -halt-on-error main.tex`
- Build artifacts are copied into `results/R00/latex_build/`, and the machine summary is written to `results/R00/rewrite_lane_summary.json`.
